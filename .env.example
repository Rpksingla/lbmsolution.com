# Copy to .env.local and fill in real values (never commit .env.local).
# These are also set in Netlify (Site settings > Environment variables).

# CMS REST API (server-only; no NEXT_PUBLIC_ prefix so the key never reaches the browser)
CMS_BASE_URL="https://api-cms.lbmdemo.com/api/v1/posts"
CMS_API_KEY=""
