# dependencies
/node_modules
# next.js
/.next
# env (never commit secrets)
.env
.env.local
.env*.local

# contact form submissions (PII) — never commit
.contact-submissions.jsonl
.lead-signups.jsonl

# macOS
.DS_Store
